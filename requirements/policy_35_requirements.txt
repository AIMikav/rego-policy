Violation: SecurityContextConstraints no longer served by v1

SecurityContextConstraints resources must use the correct API version. OCP4.x expects security.openshift.io/v1 instead of the deprecated v1 API version.
