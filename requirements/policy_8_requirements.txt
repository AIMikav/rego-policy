Violation: Container livenessProbe and readinessProbe are equal

Container livenessProbe and readinessProbe must not be identical. Having the same probe configuration for both liveness and readiness checks is an anti-pattern and should be avoided for proper health checking.
